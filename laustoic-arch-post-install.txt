install yay
sudo pacman -S --needed git base-devel
git clone https://aur.archlinux.org/yay.git
cd yay
makepkg -si

install zen browser
yay -S zen-browser-bin

install flatpak
sudo pacman -S flatpak

install shelly
yay -S shelly

install linuxtoys
curl -fsSL https://linux.toys/install.sh | bash

on linuxtoys, install:
jetbrainsmono nerd font
davinci resolve

install goverlay
sudo pacman -S goverlay

dependencies for goverlay
sudo pacman -S vulkan-tools

btrfs-assistant
sudo pacman -S btrfs-assistant

fix adwaita qt6 apps
yay -S adwaita-qt6

poolkit issues
sudo pacman -S polkit-gnome

fix gufw
xhost +SI:localuser:$(id -un) &
xhost +SI:localuser:root &

install obs studio
sudo pacman -S obs-studio obs-studio-plugin-browser

11. fix obs frontend-tools popup
sudo pacman -S luajit

install obs vkcapture
yay -S obs-vkcapture-git

12. install vlc
sudo pacman -S vlc vlc-plugins-all

install kdeconnect
sudo pacman -S kdeconnect

allow kdeconnect from ufw
sudo ufw allow 1714:1764/udp
sudo ufw allow 1714:1764/tcp

install falcond
yay -S falcond falcond-gui
sudo systemctl enable --now falcond

fix falcond group
sudo groupadd falcond
sudo usermod -aG falcond laustoic

verify
groups
laustoic gamemode libvirt input wheel falcond

install kitty
sudo pacman -S kitty

install fish
sudo pacman -S fish

add fish to standard shells
echo "$(which fish)" | sudo tee -a /etc/shells

set fish as your default shell
chsh -s $(which fish)

download inter font
https://fonts.google.com/specimen/Inter

install steam
sudo pacman -S steam

fix steam error
cd ~
rm -rf .steam

grub snapshots
sudo pacman -S grub-btrfs
sudo systemctl enable --now grub-btrfsd

snap pac + grub
sudo pacman -S snap-pac
yay -S snap-pac-grub

update grub
sudo grub-mkconfig -o /boot/grub/grub.cfg


install niri
sudo pacman -Syu niri xwayland-satellite xdg-desktop-portal-gnome xdg-desktop-portal-gtk alacritty matugen cava qt6-multimedia-ffmpeg
